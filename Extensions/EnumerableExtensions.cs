using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using WebTestProteus.Models.DataJson;

namespace WebTestProteus.Extensions
{
    public static class EnumerableHelper
    {
        //public static object[][] ToArrayPointTimeSeries<TSource>(this IEnumerable<TSource> source) where TSource : DataPointTimeSeries<X, Y>
        //{
        //    return source.Select(p => new object[] { p.Value, p.Measure }).ToArray();
        //}
    }
}
